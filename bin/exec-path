#!/usr/bin/env bash

install_path=$1
cmd=$2
executable_path=$3

if [[ "$cmd" == "play" ]]; then

	play_bin_cmd="$cmd"

	if [[ -f "$install_path/$play_bin_cmd" ]]; then
		executable_path="$play_bin_cmd"
	fi

fi

echo "$executable_path"
